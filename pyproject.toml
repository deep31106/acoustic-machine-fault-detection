[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "acoustic-machine-fault-detection"
version = "0.1.0"
description = "Acoustic machine fault detection using signal processing and machine learning"

[tool.setuptools.packages.find]
where = ["src"]